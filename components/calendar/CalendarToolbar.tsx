'use client'
import { ChevronDown, Copy, Eye, Gauge, RefreshCw } from 'lucide-react'
import { useEffect, useRef, useState } from 'react'
import type { CalendarSettings, Layer } from '@/lib/calendar/types'

type Props={layer:Layer;quote:string;settings:CalendarSettings;onQuote:(v:string)=>void;onNextQuote:()=>void;onCopyPlan:()=>void;onPatch:(p:Partial<CalendarSettings>)=>void}
export function CalendarToolbar(p:Props){const [quote,setQuote]=useState(p.quote);const [open,setOpen]=useState(false);const menuRef=useRef<HTMLDivElement>(null);useEffect(()=>setQuote(p.quote),[p.quote]);useEffect(()=>{const close=(e:PointerEvent)=>{if(!menuRef.current?.contains(e.target as Node))setOpen(false)};window.addEventListener('pointerdown',close);return()=>window.removeEventListener('pointerdown',close)},[]);const commitQuote=()=>{if(quote!==p.quote)p.onQuote(quote)};return <div className="calendar-toolbar"><div className="quote-editor"><input aria-label="Weekly quote" value={quote} onChange={e=>setQuote(e.target.value)} onBlur={commitQuote} onKeyDown={e=>{if(e.key==='Enter'){commitQuote();e.currentTarget.blur()}if(e.key==='Escape'){setQuote(p.quote);e.currentTarget.blur()}}}/><button aria-label="Load next quote" title="Load next quote" onClick={p.onNextQuote}><RefreshCw size={12}/></button></div><div>{p.layer==='actual'&&<button className="fill-plan-button" onClick={p.onCopyPlan}><Copy size={13}/>Fill from plan</button>}<label className="top-density"><Gauge size={13}/><input aria-label="Hour density" type="range" min="0.7" max="1.5" step="0.05" value={p.settings.hourScale} onChange={e=>p.onPatch({hourScale:+e.target.value})}/><small>{p.settings.hourScale.toFixed(2)}×</small></label><div className="display-menu-wrap" ref={menuRef}><button className={`toolbar-menu ${open?'active':''}`} onClick={()=>setOpen(v=>!v)}>Display options <ChevronDown size={13}/></button>{open&&<div className="display-popover"><label><span><Eye size={13}/>Show weekends</span><input type="checkbox" checked={p.settings.showWeekends} onChange={e=>p.onPatch({showWeekends:e.target.checked})}/></label><label><span>Time format</span><select value={p.settings.timeFormat} onChange={e=>p.onPatch({timeFormat:e.target.value as '12h'|'24h'})}><option value="12h">12 hour</option><option value="24h">24 hour</option></select></label><label className="display-density"><span>Hour density</span><input type="range" min="0.7" max="1.5" step="0.05" value={p.settings.hourScale} onChange={e=>p.onPatch({hourScale:+e.target.value})}/><small>{p.settings.hourScale.toFixed(2)}×</small></label></div>}</div></div></div>}
