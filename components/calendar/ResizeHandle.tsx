'use client'
import { useRef } from 'react'
type Props={side:'left'|'right';value:number;min:number;max:number;onResize:(value:number)=>void;onCommit?:(value:number)=>void}
export function ResizeHandle({side,value,min,max,onResize,onCommit=()=>{}}:Props){const start=useRef({x:0,value});const bounded=(next:number)=>Math.max(min,Math.min(max,next));const begin=(e:React.PointerEvent)=>{e.preventDefault();start.current={x:e.clientX,value};let latest=value;const move=(event:PointerEvent)=>{const delta=event.clientX-start.current.x,onward=side==='left'?delta:-delta;latest=bounded(start.current.value+onward);onResize(latest)};const up=()=>{window.removeEventListener('pointermove',move);window.removeEventListener('pointerup',up);document.body.classList.remove('resizing-sidebar');onCommit(latest)};document.body.classList.add('resizing-sidebar');window.addEventListener('pointermove',move);window.addEventListener('pointerup',up)};const key=(e:React.KeyboardEvent)=>{let next=value;if(e.key==='ArrowLeft')next=bounded(value+(side==='left'?-10:10));else if(e.key==='ArrowRight')next=bounded(value+(side==='left'?10:-10));else return;e.preventDefault();onResize(next);onCommit(next)};return <div className={`sidebar-resize-handle ${side}`} role="separator" aria-label={`Resize ${side} sidebar`} aria-orientation="vertical" aria-valuemin={min} aria-valuemax={max} aria-valuenow={Math.round(value)} tabIndex={0} onPointerDown={begin} onKeyDown={key}/>} 
